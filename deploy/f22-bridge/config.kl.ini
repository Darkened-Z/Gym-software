; ============================================================
;  KL FITNESS — ready-to-use config. Rename this to config.ini
;  Starts in SAFE monitor mode (changes nothing until you switch to enforce).
; ============================================================

[device]
ip = 192.168.18.16
port = 4370
password = 0
force_udp = false
ommit_ping = false

[cloud]
base_url = https://kl.gym.goxx.app
device_key = 5dc74f8e311f94edbaa5aa36d8cafdc33ec88ae07fcc18fe
device_sn = KL-F22

[bridge]
; SAFE default — watch the logs, confirm the right members show as "would block",
; THEN change this to  enforce  and restart.
mode = monitor
max_block_pct = 60
report_scans = false

sync_interval = 45
heartbeat_interval = 30
template_cache_dir = ./cache
state_file = ./state.json
